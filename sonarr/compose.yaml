networks:
  nginx-proxy-manager:
    external: true

services:
  sonarr:
    image: lscr.io/linuxserver/sonarr:latest
    container_name: sonarr
    restart: unless-stopped
    environment:
      - PUID=950
      - PGID=3000
      - TZ=${TZ:-Etc/UTC}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./config:/config
      - /mnt/storage1/media:/data/media
      - /mnt/storage0/downloads/qbittorrent/completed:/downloads/completed
    networks:
      - nginx-proxy-manager
    labels:
      glance.name: Sonarr
      glance.icon: si:sonarr
      glance.description: Shows Collection Manager
      glance.url: https://sonarr.kamaji.gerardgascon.com
      glance.category: arr-stack
      glance.hide: false
      glance.id: sonarr